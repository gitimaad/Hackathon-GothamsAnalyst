# Creating a database so that we can connect our Python file. 
CREATE DATABASE aviation_database;

# Using the database.
USE aviation_database;

# Selecting the database to see if the the dataset has been added. 
SELECT * FROM aviation_table;